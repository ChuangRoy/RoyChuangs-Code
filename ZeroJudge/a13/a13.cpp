#include <iostream>
using namespace std;

int main () {
    string a;
    string b;
    int A, B;
    cin >> a;
    while (a != "#") {
        cin >> b;
        cout << a << " " << b << endl;
        for (int i = 0; i < a.length(); i++) {
            switch (a[i]) {
                case 
            }
        }
        cin >> a;
    }
    return 0;
}
